{
  "name": "rrtm-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "copy:wasm": "rm -rf node_modules/rrtm && cp -r ../pkg ./node_modules/rrtm",
    "dev": "npm run copy:wasm && vite",
    "build": "npm run copy:wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "comlink": "^4.4.2"
  }
}
